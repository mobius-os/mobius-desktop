{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "launcher",
  "description": "Native capabilities for the bundled Möbius launcher only.",
  "windows": ["launcher"],
  "permissions": ["core:default"]
}
